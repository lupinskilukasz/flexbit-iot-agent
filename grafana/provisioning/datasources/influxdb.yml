apiVersion: 1

datasources:
  - name: InfluxDB
    uid: influxdb-telemetry
    type: influxdb
    access: proxy
    url: http://influxdb:8181
    isDefault: true
    jsonData:
      version: SQL
      dbName: telemetry
      httpMode: POST
      insecureGrpc: true
